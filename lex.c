#include <stdio.h>
#include <stdlib.h>

#include "lex.h"
#include "jeton.h"

/*int lex(char[], typeToken t[]){
    int i=0;
    while(str[i] != '\0'){
        switch(str[i]){
            case '+' : t[j].lexem = OPER;
            t[j].valor.ope = PLUS;
            [...];
            default : {
                if((str[i]<'z') && (str[i]>'a'){
                    [...];
                }
                if((str[i]<'9') && (str[i]>'0') || (str[i]=='.')){
                    [...];
                }
            }
        }
    }
}*/

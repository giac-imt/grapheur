#include <stdio.h>
#include <stdlib.h>

#include "lex.h"
#include "jeton.h"

#include "lex.c"

int main(){
    char str[20];
    gets(str);
    /*typeToken tab[20];
    int e;
    e = lex(str, tab);*/
    return 0;
}
